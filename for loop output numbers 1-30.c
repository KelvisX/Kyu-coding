//for loop - output numbers 1-30
#include<stdio.h>

int main() {
    int i, sum=0;
    for(i=15;i>0;i--) {
        printf("%d \n", i*iy);
        sum = sum + i;
    }
    printf("The sum is %d ", sum);
      return 0;
}
